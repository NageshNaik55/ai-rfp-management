{
  "name": "ai-rfp-management",
  "version": "1.0.11",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -k -n FE,BE \"npm:dev --workspace frontend\" \"npm:dev --workspace backend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
